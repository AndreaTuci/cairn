# npm 10.x has an arborist bug that crashes while resolving Nuxt's peer graph
# ("Cannot read properties of null (reading 'edgesOut')"). This flag walks around it.
# npm >= 11 fixes it properly and this file can go.
legacy-peer-deps=true
