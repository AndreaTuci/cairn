# Copilot instructions — <project>

All project instructions live in [AGENTS.md](../AGENTS.md), the single source shared by every
coding agent (Claude, Copilot, and any other). Do not add instructions here — edit AGENTS.md
instead.

Read `../AGENTS.md` before making any change. For frontend work, read the `ui-composition` skill
in `.github/skills/ui-composition/` and the project contract in `../design/UI-STACK.md`.
